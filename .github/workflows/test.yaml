---
name: 🧪 Testing Stuff

on: push
jobs:
    loggit:
        name: 🪵 Loggit
        runs-on: ubuntu-latest
        steps:
            -
                name: ✍️ Logging it
                run: |
                    echo "${MY_OWN_ENV_VAR} Workflow: ${GITHUB_WORKFLOW}"
                env:
                    MY_OWN_ENV_VAR: Gerald

...